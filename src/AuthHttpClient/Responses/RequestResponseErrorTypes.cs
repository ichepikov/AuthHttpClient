namespace AuthHttpClient.Responses
{
    public enum RequestResponseErrorTypes
    {
        None,
        Connection,
        Server,
        Parse,
        Authentication,
        Timeout,
        Unknown,
    }
}
